handler: lambda_function.lambda_handler
timeout: 900
module: testing_functions

package:
  patterns:
    - testing_functions/lambda_function.py

name: ${self:service}-testing_functions-${self:provider.stage}

